#include "main.h"

/**
 * is_lower - checks if character is lowercase
 * @c: character argument
 * Return: returns 1 and 0 based on condition
 */
int is_lower(int c)
{
return (c >= 'a' && c <= 'z');
}
